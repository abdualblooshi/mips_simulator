int main() {
    int number = 5; 

    // check if the number is even or odd using modulo operator
    if (number % 2 == 0) { 
        printf("%d is even\n", number);
    } else {
        printf("%d is odd\n", number);
    }

    return 0;
}
